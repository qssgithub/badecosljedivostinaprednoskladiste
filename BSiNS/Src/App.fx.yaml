App As appinfo:
    BackEnabled: =true
    Formulas: |
        =conPrimary = ColorValue("#0AA9FF");
        conAccent = ColorValue("#676767");
        /*conUser = LookUp(
            Contacts,
            Email = User().Email
        );*/
        
        logedUser = "anisaaa";
        //LookUp(Contacts, User().Email = Email).'User Name';
        
        warehouseCode = "SGP-DZ";
        isMobile = If(
            App.Width > 600,
            false,
            true
        );
        ReadyForProcessing = "ReadyForProcessing";
        InProgress = "InProgress";
        ReadyForNAVProcessing = "ReadyForNavProcessing";
        
        ReturnTypes = ["SPO","Ostalo"];
    OnStart: =
    StartScreen: =
    Theme: =RedTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

